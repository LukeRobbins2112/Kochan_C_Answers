#include <stdio.h>

int main(){

  int number;
  int right_digit;

  printf("Please enter a number: ");
  scanf("%d", &number);

  while (number != 0){
    right_digit = number % 10;
    printf("%d", right_digit);
    number /= 10;
  }

  printf("\n");

  return 0;
  




}
